# Local folder rename

GitHub repo was renamed to **`phoenix-wp-gm-dhl-mc-fix`**.

If this folder is still named `phoenix-wp-bridge-german-market-wcml`, rename it once nothing locks the directory (close Cursor/terminals in this path):

```powershell
cd C:\Users\mail\OneDrive\Desktop\Phoenix-WP
Rename-Item -LiteralPath phoenix-wp-bridge-german-market-wcml -NewName phoenix-wp-gm-dhl-mc-fix
```

Then reopen the workspace from `phoenix-wp-gm-dhl-mc-fix/`.
